# 📱 跨平台内容格式规则

## 问题根源

VitePress 生成的 HTML 中使用了特殊格式（ASCII 表格、代码块、特殊字符），在不同平台的编辑器中显示效果不一致：

- ❌ **微信公众号编辑器** - 不兼容复杂的 HTML 结构和特殊字符
- ❌ **掘金编辑器** - 对某些 HTML 标签支持不完全
- ❌ **CSDN 编辑器** - 对 CSS 和布局的支持有限
- ❌ **知乎编辑器** - 基于富文本，对某些格式不支持

## 跨平台兼容性规则

### ❌ 禁止使用的格式

| 格式类型 | 原因 | 平台影响 |
|---------|------|---------|
| **代码块内的多行 ASCII 图** | HTML 转换异常，换行混乱 | 全部平台 |
| **代码块内的流程箭头** | 字符编码问题，显示错位 | 微信、掘金 |
| **代码块内的表格** | 平台不兼容，排版错乱 | 全部平台 |
| **代码块内的特殊符号** | ↓ ↑ ← → 等在有些平台乱码 | 微信 |
| **过深的 HTML 嵌套** | 某些平台会自动删除 | CSDN、知乎 |
| **内联 HTML 标签** | 平台编辑器不兼容 | 全部平台 |

**具体例子：**
```markdown
❌ 错误：
```
浏览商品 → 加入购物车 → 生成订单
   ↓
商户确认 → 打包发货
```

❌ 错误：
```
┌─────────────┐
│  用户界面  │
└─────────────┘
```
```

### ✅ 推荐的格式

#### 规则 1️⃣：流程图 → 纯文本描述

```markdown
✅ 正确：

**第一步：用户下单** 浏览商品 → 加入购物车 → 生成订单

**第二步：确认发货** 商户确认 → 打包发货
```

**为什么？**
- 纯文本格式，所有平台都支持
- 清晰的标签说明每个步骤
- 避免了特殊字符和换行问题

---

#### 规则 2️⃣：ASCII 表格 → Markdown 表格

```markdown
❌ 错误：
```
┌─────────────────────┐
│   用户界面层        │
├─────────────────────┤
│ PC │ H5 │ 小程序 │
└─────────────────────┘
```

✅ 正确：

| 层级 | 组件 | 技术 |
|------|------|------|
| 用户界面层 | PC 管理端 | Vue + Element |
| | H5 商城 | UniApp |
| | 小程序和App | UniApp |
```

**为什么？**
- Markdown 表格是标准格式，所有平台都支持
- 结构清晰，易于理解
- 自动适配平台宽度

---

#### 规则 3️⃣：多级列表 → 分层标题 + 简单列表

```markdown
❌ 错误：
```
1️⃣ 完善商品分类和属性 - 给商品分个类
2️⃣ 上传初期商品（100+ SKU）- 没有商品卖什么呢
3️⃣ 配置优惠券、积分等营销工具 - 优惠要舍得下
```

✅ 正确：

1. **完善商品分类和属性** - 给商品分个类
2. **上传初期商品（100+ SKU）** - 没有商品卖什么呢
3. **配置优惠券、积分等营销工具** - 优惠要舍得下
```

**为什么？**
- emoji 在有些平台编码异常
- 标准 Markdown 列表兼容性最好
- 粗体突出关键词，更易阅读

---

#### 规则 4️⃣：复杂架构图 → 分层文字描述

```markdown
❌ 错误（ASCII 架构图）：
```
┌───────────────┐
│  用户界面层   │
├───────────────┤
│ PC │ H5 │ APP│
└───────────────┘
     ↓ API
┌───────────────┐
│ 后端服务层    │
└───────────────┘
```

✅ 正确（分层文字描述）：

**用户界面层：**
- PC 管理端（Vue + Element）
- H5 商城（UniApp）
- 小程序和 App（UniApp）

**API 层：** HTTPS API 通信

**后端服务层：**
- Admin Service（管理后台）
- Front Service（商城前台）

**数据存储层：**
- MySQL 5.7+ （关系数据库）
- Redis 5+ （缓存系统）
- 文件存储（OSS/本地）
```

**为什么？**
- 避免了特殊字符和换行问题
- 层级清晰，易于理解
- 所有平台都能正确显示

---

### 🎯 通用原则

| 原则 | 说明 | 优先级 |
|------|------|--------|
| **简化优先** | 用最简单的格式表达 | ⭐⭐⭐ |
| **纯文本优先** | 尽量使用纯文本，避免特殊字符 | ⭐⭐⭐ |
| **标准格式优先** | 使用 Markdown 标准格式 | ⭐⭐⭐ |
| **标签清晰** | 用粗体或标题标注含义 | ⭐⭐ |
| **逻辑完整** | 确保所有平台都能理解 | ⭐⭐ |

---

## 📋 格式检查清单

在发布到各平台前，检查以下项目：

- [ ] 没有代码块内的 ASCII 图形
- [ ] 没有代码块内的流程箭头和特殊字符
- [ ] 所有表格都使用 Markdown 格式
- [ ] 列表使用标准编号或符号（不含 emoji）
- [ ] 架构图改为分层文字描述
- [ ] 多行流程用标签分步骤说明
- [ ] 所有链接都有清晰的文字
- [ ] 没有内联 HTML 标签

---

## 🔄 自动化检测方案

可以在 `formatters.ts` 中添加检测函数：

```typescript
function validateCrossPlatformFormat(html: string): {
  issues: string[];
  isValid: boolean;
}
```

检测项目：
- 代码块中的特殊字符（↓ → ← ↑ ┌ ┐ └ ┘）
- 代码块中的换行符超过 2 个
- 非 Markdown 表格
- 内联 HTML 标签

---

## 📚 平台特定注意事项

### 📱 微信公众号
- 最严格的平台
- 不支持某些 HTML 属性
- 特殊字符容易出现编码问题
- **建议：** 使用最简单的格式

### ⚡ 掘金
- 支持 Markdown 比较完全
- 对代码块友好
- **建议：** 代码块可以保留，但避免特殊字符

### 📝 CSDN
- 支持复杂 HTML，但有限制
- 表格显示不错
- **建议：** 使用标准 Markdown 表格

### 🎓 知乎
- 基于富文本编辑器
- 兼容性很好
- **建议：** 可以使用相对复杂的格式

---

## 🎓 最佳实践示例

### ❌ 不好的例子

```markdown
### 系统架构

```
┌─────────────────────────────────────────────────────┐
│                    👤 用户界面层                    │
├─────────────────────────────────────────────────────┤
│  💻 PC 管理端      │  📱 H5 商城      │  📲 小程序  │
│  (Vue + Element)   │   (UniApp)       │  (UniApp)   │
└─────────────────────────────────────────────────────┘
                    ↓ HTTPS API
┌─────────────────────────────────────────────────────┐
│          ⚙️ 后端服务层（Java SpringBoot）         │
├─────────────────────────────────────────────────────┤
│  Admin Service    │      Front Service              │
└─────────────────────────────────────────────────────┘
```
```

**问题：**
- 微信公众号：表格错位，特殊字符乱码
- 掘金：换行混乱
- CSDN：符号显示异常
- 知乎：勉强可用

---

### ✅ 好的例子

```markdown
### 系统架构

**用户界面层：** 通过 HTTPS API 与后端通信
- PC 管理端（Vue + Element）- 管理员使用
- H5 商城（UniApp）- 用户通过浏览器访问
- 小程序和 App（UniApp）- 用户通过小程序和 App 访问

**后端服务层：** Java SpringBoot 框架
- Admin Service（管理后台）- 处理管理功能
- Front Service（商城前台）- 处理用户请求

**数据存储层：**
- MySQL 5.7+（关系数据库）- 业务数据存储
- Redis 5+（缓存系统）- 会话和缓存管理
- 文件存储（OSS/本地）- 图片和文件
```

**优势：**
- 所有平台显示完全一致 ✓
- 清晰易读 ✓
- 结构清楚 ✓
- 易于维护 ✓

---

## 🚀 实施步骤

1. **立即**：更新 `formatters.ts`，在转换时检查格式问题
2. **短期**：审查所有现有文档，按规则调整
3. **长期**：建立内容创作指南，新文档按规则编写

---

## 💡 为什么这样做？

✅ **一致性** - 所有平台显示效果相同
✅ **可维护性** - 不需要为每个平台单独调整
✅ **可扩展性** - 添加新平台时无需修改文档
✅ **用户体验** - 清晰易读，专业美观

---

这是一个**系统性的解决方案**，而不是补丁式的修复！

