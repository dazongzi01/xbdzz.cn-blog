# CRMEB 文档平台 - 初始化指南

## 现状

你已经拥有一个完整的项目框架，包含：

1. ✅ **爬虫工具** (`crawler/`)
   - `spider.py` - 主爬虫程序
   - `parser.py` - HTML 解析器
   - `cleaner.py` - 内容清洁器
   - `config.json` - 配置文件

2. ✅ **项目结构和任务清单** (`.claude/todos.md`)

## 下一步

### 步骤 1：准备爬虫环境（预计 10 分钟）

```bash
cd crmeb-docs-platform/crawler
pip install -r requirements.txt
```

### 步骤 2：分析你的网站结构（预计 20 分钟）

由于我无法直接访问你的 doc.crmeb.com，我需要你做以下事情：

#### 2.1 访问你的网站首页
打开：https://doc.crmeb.com/crmebjavalandmer/CRMEBjava/25961

#### 2.2 分析页面结构
请回答以下问题（复制你看到的 HTML 或提供截图）：

1. **导航菜单在哪里？**
   - 左侧边栏
   - 顶部导航
   - 两者都有

2. **原始的模块结构是什么？**
   - 例如：模块 1 > 章节 A > 页面 1
   - 你能否列举出最顶层的 3-5 个模块名称？

3. **单个文档页面的内容结构**
   - 标题在哪里（h1, h2 还是其他）
   - 主要内容在什么容器中（main, article, .content 等）
   - 有侧边栏、页脚或其他需要排除的部分吗

#### 2.3 或者：直接提供网站的爬虫脚本

如果你的网站已经有现成的爬虫脚本或数据导出功能，请提供！

### 步骤 3：定制爬虫（如果需要）

根据你提供的信息，我会调整：
- `spider.py` 中的链接识别逻辑
- `parser.py` 中的内容提取选择器
- `config.json` 中的爬虫参数

### 步骤 4：运行爬虫

```bash
python spider.py
```

输出会生成在 `output/` 目录中

## 备选方案

如果爬虫无法工作，你也可以：

1. **手动导出数据**
   - 如果你的 CMS 系统支持数据导出，请导出为 JSON 或 CSV
   - 我可以编写脚本将其转换为标准的 Markdown 文件

2. **使用现有的备份**
   - 如果你有文档的备份（数据库、文件等），我们可以直接使用

3. **逐步迁移**
   - 先手动迁移关键文档
   - 再逐步自动化其他内容

## 项目时间线

假设爬虫成功工作，完整的迁移过程预计需要：

- **阶段 1**（爬虫）：4-6 小时
- **阶段 2**（VitePress 搭建）：3-4 小时
- **阶段 3**（多平台发布系统）：6-8 小时
- **阶段 4**（测试和部署）：2-3 小时

**总计**：15-21 小时的开发工作

## 下一步行动

请提供以下信息之一：

1. **站点 HTML 示例**
   - 打开浏览器开发者工具，查看一个文档页面的 HTML
   - 复制一部分关键的 HTML 结构

2. **截图**
   - 网站的首页截图，显示导航结构

3. **网站的技术栈**
   - 你使用的是什么 CMS？（自开发、WordPress、Confluence 等）
   - 是否有现成的 API？

4. **数据导出**
   - 是否有数据导出功能？

一旦你提供了这些信息，我将立即：
1. 调整爬虫配置
2. 运行爬虫爬取所有内容
3. 继续搭建 VitePress 文档系统
4. 开发多平台发布工具

期待你的回复！
