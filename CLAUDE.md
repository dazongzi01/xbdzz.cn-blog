# 项目规范

## 文档写作风格

这是一个 VitePress 文档站点，面向商城系统的运营人员和开发者。

### 写作原则

1. **技术人的幽默感** 🤓
   - 用程序员能懂的梗和比喻（比如「给商品穿个节日限定皮肤」）
   - 适当吐槽，但不低俗
   - 用引用块 `>` 加入调侃和灵魂拷问

2. **小红书风格的活泼感** ✨
   - 每篇开头来一句金句或调侃
   - 多用「这是个啥」「干嘛用的」这种口语化表达
   - 适当使用 emoji 增加趣味性 🎯

3. **保持专业不媚俗** 💼
   - 信息准确，逻辑清晰
   - 不过度堆砌 emoji，点到为止
   - 关键信息用 `:::tip` `:::warning` `:::danger` 强调

### 格式规范

- 路径用 **粗体**：`**路径**：平台后台 → 营销 → xxx`
- 步骤用 `Step 1` / `Step 2` 而不是「第一步」
- 表格列名口语化：「这是干嘛的」「干嘛用的」
- 代码/配置项用反引号包裹

### 示例

```markdown
# 某功能管理

> 一句话调侃或金句 🎯

## 这是个啥？

用通俗的话解释这个功能的本质...

**路径**：平台后台 → xxx → xxx

### 配置项说明

| 参数 | 这是干嘛的 |
| --- | --- |
| xxx | 解释... |

::: tip 小技巧
一些实用建议
:::
```

## HTML 转 Markdown 工作流

处理 `/Users/dazongzi/Downloads/MyDoc/` 下的 HTML 文件时：
1. 用 Python 提取正文内容（跳过导航菜单和评论区）
2. 过滤掉无用图片（dark_logo.png、isSupport.png 等 UI 图标）
3. 复制图片到 `docs/03_使用手册/images/` 目录
4. 更新 `docs/.vitepress/config.ts` 侧边栏配置
