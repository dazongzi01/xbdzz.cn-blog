# 📱 多平台内容发布工具 - 使用指南

## 系统概述

这是一个内置于 VitePress 文档网站中的**多平台格式化工具**。它可以将你的文档自动转换为各个平台的最佳格式，一键复制粘贴到目标平台。

### 支持的平台

- 📱 **微信公众号** - 内联 CSS 样式，保留排版效果
- ⚡ **掘金** - 简洁的现代风格
- 📝 **CSDN** - 传统富文本格式
- 🎓 **知乎** - 知乎编辑器兼容格式

## 快速开始

### 步骤 1️⃣: 启动开发服务器

```bash
npm run dev
```

服务器将在 `http://localhost:5173/` 启动

### 步骤 2️⃣: 访问任意文档（添加魔法参数）

在浏览器地址栏中，在 URL 后面添加 `?dazongzi=666`：

```
http://localhost:5173/01_快速开始/01_系统介绍?dazongzi=666
```

**注意：必须带上魔法参数才能看到工具栏！**

### 步骤 3️⃣: 看到工具栏

页面右上角会出现平台按钮工具栏：

```
┌─────────────────────────────┐
│ 📱 微信公众号  ⚡ 掘金     │
│ 📝 CSDN     🎓 知乎        │
└─────────────────────────────┘
```

### 步骤 4️⃣: 点击平台按钮预览

点击任何平台按钮（例如 📱 微信公众号），会弹出预览窗口：

- 展示该平台的格式化效果
- 所见即所得
- 点击「📋 复制全文」按钮

### 步骤 5️⃣: 粘贴到目标平台编辑器

- 打开对应平台的编辑器（微信公众号后台、掘金编辑器等）
- 在文本框中 `Ctrl+V` 或 `Cmd+V` 粘贴
- 完成！✅

## 工作原理

### 功能流程

```
你的文档（Markdown HTML）
    ↓
VitePress 渲染为网页 HTML
    ↓
PlatformToolbar 组件检测 ?dazongzi=666 参数
    ↓
用户点击平台按钮
    ↓
formatters.ts 应用平台特定样式
    ↓
预览窗口显示格式化结果
    ↓
用户复制内容
    ↓
粘贴到目标平台编辑器
```

### 各平台的格式化策略

**微信公众号 (formatForWechat)**
- ✅ 内联所有 CSS 样式（微信编辑器要求）
- ✅ 绿色标题，左边框装饰
- ✅ 代码块带浅色背景
- ✅ 图片居中显示
- ✅ 保留超链接格式
- ⚠️ 图片需要手动上传到微信图床

**掘金 (formatForJuejin)**
- ✅ 蓝色标题，简洁风格
- ✅ 代码块用掘金风格
- ✅ 支持表格和列表
- ✅ 引用块带蓝色背景

**CSDN (formatForCSDN)**
- ✅ 传统富文本格式
- ✅ 橙红色标题，下划线装饰
- ✅ 代码块带浅色背景
- ✅ 完整的 HTML 结构

**知乎 (formatForZhihu)**
- ✅ 简洁的知乎风格
- ✅ 标题带底线
- ✅ 深灰色文本
- ✅ 知乎编辑器兼容格式

## 详细使用步骤

### 完整示例：发布到微信公众号

#### 1. 访问文档页面（带参数）

```
http://localhost:5173/01_快速开始/01_系统介绍?dazongzi=666
```

#### 2. 等待页面加载

页面加载完成后，你会在右上角看到工具栏（固定位置）

#### 3. 点击「📱 微信公众号」按钮

- 弹出预览窗口
- 显示该文档在微信中的样子
- 你可以滚动查看全部内容
- 确认格式无误

#### 4. 点击「📋 复制全文」

按钮会临时变成「✅ 已复制！」，表示内容已经复制到剪贴板

#### 5. 打开微信公众号后台

访问：`https://mp.weixin.qq.com/`

#### 6. 新建图文消息

- 点击「新建」或「群发」
- 选择「新建图文消息」
- 进入编辑器

#### 7. 粘贴内容

在编辑器的文本框中：
- `Ctrl+V`（Windows）或 `Cmd+V`（Mac）粘贴
- 内容会保持格式化效果
- 调整图片和其他细节（如有需要）

#### 8. 完成编辑并发布

- 检查无误后点击发送
- 完成！🎉

### 关键说明

#### ⚠️ 图片处理

**现状：**
- 工具复制的内容中，图片保留原始路径
- 图片需要你手动上传到各平台

**操作步骤：**
1. 从文档中复制图片路径：`docs/...`
2. 在 Markdown 中找到图片文件
3. 使用平台的图片上传功能上传
4. 或者上传到图床后手动替换链接

#### 🔒 隐藏操作（魔法参数）

工具栏**只在**添加 `?dazongzi=666` 参数时才会出现：

```
✅ 带参数（显示工具栏）
http://localhost:5173/01_快速开始/01_系统介绍?dazongzi=666

❌ 不带参数（隐藏工具栏）
http://localhost:5173/01_快速开始/01_系统介绍
```

这样可以保持网站的普通文档风格，工具栏只在需要时出现。

#### 📋 复制效果

复制时会尝试复制：
1. **HTML 格式**（保留样式和排版）
2. **降级方案**：如果浏览器不支持富文本复制，会复制纯文本

大多数编辑器会优先使用 HTML 格式，保留你的样式。

## 平台特点和注意事项

### 微信公众号

**优势：**
- 样式保留最完整
- 绿色主题颜色
- 左边框装饰更专业

**注意：**
- 必须手动上传图片
- 超链接会保留但需要检查
- 复杂样式可能需要微调

### 掘金

**优势：**
- 支持 Markdown 和 HTML
- 社区活跃，互动多
- 推荐算法好

**注意：**
- 掘金偏向技术文章
- 可能需要添加相关标签
- 粘贴后可能自动重新排版

### CSDN

**优势：**
- 传统富文本格式，兼容性好
- SEO 权重高
- 用户多

**注意：**
- 样式可能显示不完整
- 建议粘贴后人工检查
- 有可能需要手动调整缩进

### 知乎

**优势：**
- 用户活跃
- 可以带来精准流量
- 社区反馈快速

**注意：**
- 知乎对格式有限制
- 某些样式可能不支持
- 建议粘贴后浏览一遍

## 故障排查

### Q: 工具栏没有出现

**A:** 检查以下几点：
1. ✅ URL 中是否有 `?dazongzi=666` 参数
2. ✅ 页面是否完全加载（检查浏览器控制台是否有错误）
3. ✅ 尝试刷新页面 `F5`
4. ✅ 清除浏览器缓存

### Q: 点击平台按钮后没有反应

**A:**
1. ✅ 打开浏览器开发者工具（F12）
2. ✅ 检查控制台是否有错误信息
3. ✅ 确保页面内容已完全加载
4. ✅ 尝试在不同的文档页面上操作

### Q: 复制后粘贴到编辑器没有格式

**A:**
1. ✅ 确保浏览器支持富文本复制（现代浏览器都支持）
2. ✅ 尝试在编辑器中使用「粘贴为纯文本」功能，然后手动调整
3. ✅ 某些编辑器可能需要特殊的粘贴快捷键

### Q: 图片显示不正确

**A:**
1. 这是正常的 - 图片需要手动上传
2. 你需要：
   - 找到原始图片文件
   - 上传到对应平台的图床
   - 或者使用外部图床服务
   - 然后更新文本中的图片链接

### Q: 某些样式在目标平台显示不对

**A:**
这是因为不同平台的编辑器支持的样式不同：

1. **建议：** 粘贴后在平台编辑器中微调
2. **方案：** 使用平台提供的富文本工具调整格式
3. **备选：** 复制文本内容，在编辑器中手动重新排版

## 技术架构

### 文件结构

```
docs/.vitepress/theme/
├── index.ts                    # 主题入口
├── style.css                   # 主题样式
├── components/
│   └── PlatformToolbar.vue    # 工具栏组件
└── utils/
    └── formatters.ts          # 格式化工具函数
```

### 核心模块

**formatters.ts**
- 包含 4 个格式化函数
- 每个函数对应一个平台
- 使用 markdown-it 库解析 HTML
- 应用平台特定的内联 CSS 样式

**PlatformToolbar.vue**
- Vue 3 组件
- 检测 `?dazongzi=666` 参数
- 管理预览窗口状态
- 处理复制到剪贴板逻辑

**index.ts**
- 注册组件到 VitePress 主题
- 使用 `doc-before` slot 插入工具栏

## 部署到生产环境

### 构建网站

```bash
npm run build
```

构建后的网站包含所有格式化功能，可以部署到任何静态服务器。

### 静态部署

```bash
# 构建生产版本
npm run build

# dist/ 目录中的文件就是可以部署的完整网站
# 上传到 GitHub Pages / Vercel / Netlify 等
```

### Docker 部署

```dockerfile
FROM node:18
WORKDIR /app
COPY . .
RUN npm install
RUN npm run build
EXPOSE 5173
CMD ["npm", "run", "dev"]
```

## 高级用法

### 自定义平台格式

如果要添加新平台或修改格式，编辑 `formatters.ts`：

```typescript
export function formatForMyPlatform(htmlContent: string): string {
  let html = md.render(htmlContent)

  // 应用你的平台特定样式
  html = html.replace(/<h2>/g, '<h2 style="...">')
  // ...其他样式

  return html
}
```

然后在 `PlatformToolbar.vue` 中注册：

```typescript
const formatters = {
  wechat: formatForWechat,
  myplatform: formatForMyPlatform,
  // ...
}
```

### 调整样式

每个平台的格式化函数中都有详细的样式定义，可以：

1. 修改颜色
2. 调整字号
3. 改变间距
4. 添加装饰元素

例如修改微信标题颜色：

```typescript
// 原始
html = html.replace(/<h2>/g, '<h2 style="color: #2c3e50; ...">')

// 修改为红色
html = html.replace(/<h2>/g, '<h2 style="color: #ff0000; ...">')
```

## 常见问题

**Q: 为什么需要魔法参数？**

A: 这样可以保持网站的普通风格，工具栏只在需要时出现。这是一个隐藏操作，不会影响普通访问者。

**Q: 能否自动上传图片？**

A: 目前不支持自动上传，原因是：
- 各平台的上传 API 都需要认证
- API 权限难申请且容易失效
- 手动上传更可靠，避免维护复杂性

**Q: 支持的图片格式有哪些？**

A: 支持所有常见格式：
- `.jpg` / `.jpeg`
- `.png`
- `.gif`
- `.webp`
- `.svg`

**Q: 复制后可以重新编辑吗？**

A: 完全可以！粘贴到编辑器后，你可以：
- 修改文字
- 调整格式
- 添加/删除图片
- 修改链接
- 应用编辑器的特殊功能

**Q: 支持多少字的文章？**

A: 没有限制。但建议：
- 微信公众号：3000 字以内效果最好
- 掘金：5000-10000 字左右
- CSDN：10000 字以上也可以
- 知乎：3000-5000 字较为合适

## 总结

这个工具的目标是：

✅ **一键转换** - 自动转换为各平台格式
✅ **所见即所得** - 预览效果准确
✅ **一键复制** - 直接粘贴到编辑器
✅ **隐藏操作** - 不影响普通阅读体验
✅ **零维护** - 纯前端实现，无需后端服务

---

**准备好开始发布了吗？** 🚀

访问任意文档页面，添加 `?dazongzi=666` 参数，然后点击平台按钮开始吧！
