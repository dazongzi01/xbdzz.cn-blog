{
  "name": "my-docs",
  "version": "1.0.0",
  "description": "大粽子全局的",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "python3 scripts/filter-collected-docs.py && vitepress build docs",
    "build:force": "vitepress build docs",
    "preview": "vitepress preview docs",
    "filter": "python3 scripts/filter-collected-docs.py"
  },
  "devDependencies": {
    "vitepress": "^1.0.0-alpha.28"
  },
  "dependencies": {
    "markdown-it": "^14.1.0"
  }
}
